<%
include("/jagg/jagg.jag");

(function () {
    response.contentType = "application/json; charset=UTF-8";
    var mod, obj, result,
            action = request.getParameter("action"),
            site = require("/site/conf/site.json"),
            msg = require("/site/conf/ui-messages.jag")
    allProviders = "__all_providers__";
    if (jagg.getUser() == null) {
        print({
            error: true,
            message: 'timeout'
        });
    } else {


        if (action === "getAPIResponseFaultCount") {
            var provider = jagg.getUser().username;
            mod=org.wso2.carbon.apimgt.usage.client.impl.UsageClient(provider).getClient();
            var from = request.getParameter("fromDate");
            var to = request.getParameter("toDate");
            var currentLocation = request.getParameter("currentLocation");
            if (currentLocation.indexOf("all-statistics") >= 0) {
                result = mod.getAPIResponseFaultCount(allProviders, from, to);
            } else {
                result = mod.getAPIResponseFaultCount(provider, from, to);
            }

            if (result.error == "true") {
                obj = {
                    error: result.error,
                    message: msg.error.backendError(action)
                };
            } else {
                obj = {
                    error: false,
                    usage: result
                }
            }
            print(obj);
        } else if (action === "getAPIFaultyAnalyzeByTime") {
            var provider = jagg.getUser().username;
            mod=org.wso2.carbon.apimgt.usage.client.impl.UsageClient(provider).getClient();
            var currentLocation = request.getParameter("currentLocation");
            if (currentLocation.indexOf("all-statistics") >= 0) {
                result = mod.getAPIResponseFaultCount(allProviders,from, to,10);
            } else {
                result = mod.getAPIResponseFaultCount(provider,from, to,10);
            }

            if (result.error == "true") {
                obj = {
                    error: result.error,
                    message: msg.error.backendError(action)
                };
            } else {
                obj = {
                    error: false,
                    usage: result
                }
            }
            print(obj);
        } else if (action === "getFirstAccessTime") {
            var provider = jagg.getUser().username;
            mod=org.wso2.carbon.apimgt.usage.client.impl.UsageClient(provider).getClient();
            var currentLocation = request.getParameter("currentLocation");
            if (currentLocation.indexOf("all-statistics") >= 0) {
                result = mod.getFirstAccessTime(allProviders);
            } else {
                result = mod.getFirstAccessTime(provider);
            }
            if (result.error == "true") {
                obj = {
                    error: result.error,
                    message: msg.error.backendError(action)
                };
            } else {
                obj = {
                    error: false,
                    usage: result.toArray()
                }
            }
            print(obj);

        }

        else if (action === "isDataPublishingEnabled") {
            result = org.wso2.carbon.apimgt.usage.client.impl.UsageClient.isDataPublishingEnabled();
            
            if (result.error == "true") {
                obj = {
                    error: result.error,
                    message: msg.error.backendError(action)
                };
            } else {
                obj = {
                    error: false,
                    usage: result
                }
            }
            print(obj);

        }
        else {
            print({
                error: true,
                message: msg.error.invalidAction(action)
            });
        }
    }
}());
%>