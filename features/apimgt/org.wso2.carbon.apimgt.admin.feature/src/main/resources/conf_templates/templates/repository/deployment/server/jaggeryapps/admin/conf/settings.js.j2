/**
 * This file contains the Admin Portal web app related configurations
 * Note: In future,this configuration will be be parameterized and manage from deployment.toml
 */
const AppConfig = {
    app: {
        context: '{{apim.admin.app.context}}', // Note the leading `/` and no trailing `/`
        customUrl: { // Dynamically set the redirect origin according to the forwardedHeader host|proxyPort combination
            enabled: {{apim.admin.app.custom_url.enabled}},
            forwardedHeader: '{{apim.admin.app.custom_url.forwarded_header}}',
        },
        origin: {
            host: '{{apim.admin.app.origin.host}}', // Used to construct the loopback origin,
            // It's very unlike you need to change this hostname,
            // It is `localhost` in 99.99% case, If you want to change server host name change it in deployment.toml
        },
        feedback: { // If enabled, Feedback form option(an icon) will be available in the footer LHS bottom
            enable: {{apim.admin.app.feedback.enabled}},
            serviceURL: '{{apim.admin.app.feedback.service_url}}', // Check `/source/src/app/components/Base/Footer/FeedbackForm.jsx` for details
        },
        singleLogout: {
            enabled: {{apim.admin.app.single_logout.enabled}}, // If enabled, user will be logged out
            // from the App when logged out from the IDP (eg: SSO logout from a different App).
            timeout: {{apim.admin.app.single_logout.timeout}}, // Defines the timeout for the above periodical session status check
        },
        docUrl: '{{apim.admin.app.docUrl}}',
    },
    idp: {
        origin: '{{apim.admin.idp.origin}}',
        checkSessionEndpoint: '{{apim.admin.idp.check_session_endpoint}}',
    },
};

if (typeof module !== 'undefined') {
    module.exports = AppConfig; // To be used in JS unit tests
}
