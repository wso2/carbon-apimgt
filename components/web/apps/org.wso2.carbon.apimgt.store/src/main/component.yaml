apis: []

bindings:
  - zoneName: footer
    mode: prepend
    fragments:
      - org.wso2.carbon.apimgt.web.store.feature.footer
  - zoneName: topbar
    mode: prepend
    fragments:
      - org.wso2.carbon.apimgt.web.store.feature.global-navigation
  - zoneName: sidebar
    mode: prepend
    fragments:
      - org.wso2.carbon.apimgt.web.store.feature.sidebar

config:
  appName : API Store
  title : "API Store Listing"
  loginRedirectUri: "/apis"
  swaggerURL : "/api/am/store/v1.0/apis/swagger.json"
  loginPageUri : "/store/auth/login"
